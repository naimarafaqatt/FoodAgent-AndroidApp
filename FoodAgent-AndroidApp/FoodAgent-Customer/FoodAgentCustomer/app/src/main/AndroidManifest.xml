<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodagentcustomer">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodAgentCustomer">
        <activity android:name=".activites.ChatActivity"></activity>
        <activity android:name=".activites.MyCartActivity" />
        <activity android:name=".activites.ProductDetailActivity" />
        <activity android:name=".activites.ProductsActivty" />
        <activity android:name=".activites.RegisterActivity" />
        <activity android:name=".activites.DashBoardActivity" />
        <activity android:name=".activites.LoginActivity" />
        <activity android:name=".activites.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".Notifications.FirebaseMessaging"
            android:exported="true"
            android:enabled="true"/>
    </application>

</manifest>